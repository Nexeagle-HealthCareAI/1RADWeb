# Loaded by Vite when the build is in development mode (e.g. `vite dev`,
# or `vite build --mode development`). Used as a defensive fallback for the
# Dev API URL. The pipeline still exports VITE_API_URL explicitly for both
# Dev and Prod builds, so this file is only a safety net.
VITE_API_URL=http://151.185.45.77:5000/api/v1
VITE_APP_URL=http://151.185.45.77:80
