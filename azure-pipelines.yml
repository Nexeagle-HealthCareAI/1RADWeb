trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - "*"

pool:
  vmImage: ubuntu-latest

variables:
  nodeVersion: '20.x'         # Node LTS

stages:
- stage: BuildAndDeploy
  displayName: Build & Deploy to Azure Static Web Apps
  jobs:
  - job: Build
    displayName: Build React
    steps:
    - checkout: self
      clean: true

    - task: NodeTool@0
      inputs:
        versionSpec: $(nodeVersion)
      displayName: Use Node $(nodeVersion)

    # (Optional) speed up builds by caching node_modules based on package-lock.json
    - task: Cache@2
      inputs:
        key: 'npm | "$(Agent.OS)" | $(Build.SourcesDirectory)/package-lock.json'
        restoreKeys: |
          npm | "$(Agent.OS)"
        path: $(Build.SourcesDirectory)/node_modules
      displayName: Cache node_modules

    - script: |
        npm ci
        npm run build
      workingDirectory: $(Build.SourcesDirectory)
      displayName: npm ci & build

    # Deploy to Azure Static Web Apps
    - task: AzureStaticWebApp@0
      displayName: Deploy to Azure Static Web Apps
      inputs:
        app_location: '.'                            # React app root (repo root)
        output_location: 'dist'                      # Vite build output folder
        api_location: ''                             # leave empty if you don't have an API
        azure_static_web_apps_api_token: '3ad2ed7d5cb0df7d75644e9bafe0a0f7368ad9f53ddd18500f295df2e40588ba03-5875f55b-cc00-44ed-90df-6e92871dbb8e000212708b1f9b00'
